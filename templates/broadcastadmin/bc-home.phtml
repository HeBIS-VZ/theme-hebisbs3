<?
$this->headTitle('VuFind Administration - ' . 'Broadcast Admin');
$this->headScript()->appendFile($this->basePath() . '/themes/hebisbs3' . '/js/admin.js');
$this->headScript()->appendFile($this->basePath() . '/themes/hebisbs3' . '/js/bootstrap2-toggle.min.js');
$this->headLink()->appendStylesheet($this->basePath() . '/themes/hebisbs3' . '/css/bootstrap2-toggle.min.css', 'all');
$lang = $this->layout()->userLang;
?>

<!--Modal-->
<div class="modal fade" id="admin-delete-confirmation" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <button type="button" class="close" data-dismiss="modal" id="cross">&times;</button>
                <div class="pull-left col-md-2" style="font-size:300%">
                    <span class="hds-icon-trash-empty"><!-- --></span>
                </div>
                <p id="admin-delete-question"><?= $this->translate('Are you sure you want to delete this?') ?><i></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default"
                        data-dismiss="modal"><?= $this->translate('Cancel') ?></button>
                <button type="button" class="btn btn-danger" data-dismiss="modal"
                        id="delete-confirmation-button"><?= $this->translate('Delete') ?></button>
            </div>
        </div>
    </div>
</div>
<!--End of Modal-->
<div class="row">
    <div class="<?= $this->layoutClass('mainbody') ?>">
        <h3><span class="hds-icon-bullseye"></span><b>Broadcasts</b></h3>
        <hr>
        <?
        $visibilitySymbols = array(
            'visible' => '<span class="hds-icon-eye green"></span>',
            'unvisible' => '<span class="hds-icon-eye-off red"></span>',
        ); ?>
        <? $oldbcid = -1; ?>

        <ul id="admin-list">
            <li>
                <div class="bc">
                    <a class="hds-icon-plus-circled"
                       href="<?= $this->url('broadcastadmin', ['action' => 'add']) ?>">
                        <i><?= $this->transEsc('Add New Broadcast') ?></i></a>
                </div>
            </li>
            <? foreach ($this->rows as $row) : ?>
                <? $bcid = $row->bcid;
                $message = $this->escapeHtml(strip_tags($row->message));
                ?>
                <li>
                    <div class="bc">
                            <span class="admin-delete-button hds-icon-trash"
                                  href="<?= $this->url('broadcastadmin', ['action' => 'delete', 'bcid' => $bcid]) ?>"
                                  data-href="<?= $message ?>"
                                  id="<?= $bcid ?>">
                            </span>
                        <span class="sp-page-visibility <?= ($row->show == 1) ? "hds-icon-eye green" : "hds-icon-eye-off red" ?>"
                              href="<?= $this->url('broadcastadmin', ['action' => 'toggleVisibility', 'bcid' => $bcid]) ?>">
                            </span>
                        <a class="hds-icon-edit"
                           href="<?= $this->url('broadcastadmin', ['action' => 'edit', 'bcid' => $bcid]) ?>"></a>
                        <span><?= $message ?></span>
                    </div>
                </li>
            <? endforeach; ?>
        </ul>

        <br>
        <hr>

        <ul id="expired-list">
            <? foreach ($this->expired as $expired) : ?>
                <? $bcid = $expired->bcid;
                $message = $this->escapeHtml(strip_tags($expired->message));
                ?>
                <li>
                    <div class="bc">
                            <span class="admin-delete-button hds-icon-trash"
                                  href="<?= $this->url('broadcastadmin', ['action' => 'delete', 'bcid' => $bcid]) ?>"
                                  data-href="<?= $message ?>"
                                  id="<?= $bcid ?>">
                            </span>
                        <a class="hds-icon-edit"
                           href="<?= $this->url('broadcastadmin', ['action' => 'edit', 'bcid' => $bcid]) ?>"></a>
                        <span id="expired-broadcast"><?= $message ?></span>
                    </div>
                </li>
            <? endforeach; ?>
        </ul>

    </div>
    <div class="<?= $this->layoutClass('sidebar') ?>">
        <?= $this->render("admin/menu.phtml") ?>
    </div>
</div>
